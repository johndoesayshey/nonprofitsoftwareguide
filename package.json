{
  "name": "nonprofitsoftwareguide",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "npm run guardrails:src && astro build && npm run guardrails:dist",
    "preview": "astro preview",
    "astro": "astro",
    "guardrails": "npm run guardrails:src && npm run guardrails:dist",
    "guardrails:src": "node scripts/check-factcheck.mjs && node scripts/check-clusters.mjs && node scripts/check-depth.mjs && node scripts/check-queries.mjs",
    "guardrails:dist": "node scripts/check-disclosure.mjs && node scripts/check-cluster-links.mjs",
    "check-freshness": "node scripts/check-freshness.mjs",
    "check-links": "node scripts/check-links.mjs",
    "owner": "node scripts/owner-dashboard.mjs",
    "edit": "node scripts/edit-server.mjs"
  },
  "dependencies": {
    "@astrojs/mdx": "^4.2.0",
    "@astrojs/rss": "^4.0.11",
    "astro": "^5.13.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.0",
    "gray-matter": "^4.0.3",
    "tailwindcss": "^4.1.0"
  }
}
